package edu.ucalgary.ensf409;
import org.junit.*;
import static org.junit.Assert.*;

public class OrderTest
{
    // input takes in wrong spelling of type input for and sends order out with the wrong spelling. (CreateOrder makes document)
    // To test this class, must test through other methods because its all in one while loop in main
}
